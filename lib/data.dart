class MentorDescription {
  String name;
  String title; // location name for UI
  String time; // the time in that location // url to an asset flag icon
  String photo;
  String meetID; // location url for api endpoint

  MentorDescription({this.name, this.title, this.time, this.photo,this.meetID});
}
